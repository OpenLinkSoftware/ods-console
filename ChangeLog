0.4
  * Allow the addition of multiple value inputs for each parameter. This allows to perform requests
    which allow or require lists like acl.groups.new.

0.3
  * Dynamic procedure list constructed via a vsp page.
  * Removed the configuration option to set the ODS host. It does break the dynamic procedure list.
